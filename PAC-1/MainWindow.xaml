<Window x:Class="PAC_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:vm="clr-namespace:PAC_1.ViewModels"
        xmlns:local="clr-namespace:PAC_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
    
        <ContentControl Content="{Binding SelectedViewModel}"/>
        <StackPanel Grid.Row="1">

            <Button Content="Zajęcia indywidualne" Width="200" Height="30" HorizontalAlignment="Left" Margin="10" Command="{Binding UpdateViewCommand}" CommandParameter="IndividualForm"/>
            <Button Content="Zajęcia grupowe" Width="200" Height="30" HorizontalAlignment="Left" Margin="10" Command="{Binding UpdateViewCommand}" CommandParameter="GroupForm"/>
        </StackPanel>

    </Grid>
</Window>
